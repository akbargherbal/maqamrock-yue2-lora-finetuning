# Tokens that legitimately resolve outside this repo — runtime artifacts on the
# Colab VM, files in the ai-toolkit / audio.cpp clones, and example inputs or
# outputs used in command illustrations. Matched with fnmatch against the raw
# token (glob syntax; `*` crosses `/`).
#
# Curate this by hand. Every entry suppresses a class of missing-path findings,
# so a broad glob here is how you silently hide real drift.

# ai-toolkit clone (/content/ai-toolkit)
run.py
jobs/process/BaseSDTrainProcess.py
toolkit/*.py
yue2_model.py

# audio.cpp clone (/content/audio.cpp)
CMakeLists.txt
build_linux.sh
scripts/build_linux.sh

# runtime artifacts under /content/
loss_log.db
train.log
config.yaml
aitk_db.db
gpu_usage.csv
run_manifest.json
*.safetensors
*.gguf

# example / user-supplied files in docs (inputs and command outputs)
my_songs.json
songs.json
a.json
b.json
input.json
songs.*.json
INFERENCE/songs.*.json
batch_manifest.json
batch_summary.txt
*.report.json
_style.txt
_lyrics.txt
_time.txt
_gpu.csv
_runs_status.log

# local-only v2 build script cited as provenance (deliberately not committed; see DECISIONS.md)
prepare_yue2_dataset_v2.py

# dataset track ids referenced inside one research report (doc-scoped `file :: glob`)
INFERENCE/yue2_eval_heldout/heldout_eval_report.md :: *.txt

# external repo's doc, cited in one file (doc-scoped)
docs/FUTURE_PRONUNCIATION_LORA.md :: docs/lora.md

# flags of external tools quoted in commands (git log --left-right/--count, cmake)
--left-right
--count
--generate-code

# prose slash-phrases and placeholders in this skill's own docs
config/hyperparameter
config/architecture
path/to/doc.md

# files created on demand (this skill's own outputs)
SOURCE_OF_TRUTH.md
RECONCILIATION_LOG.md
.reconcile/*
